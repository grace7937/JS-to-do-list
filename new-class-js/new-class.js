class Todo {

  constructor(text,id) {
    this.todos = {};
    }

    addTodo(text){
      this.todos ={
        text: text,
        id : todos.length === 0 ? 1 : todos[todos.length - 1].id + 1,
    }
    this.todos.push(todo);
  }

  
class View {

this.input = document.querySelector('#input');


this.input.addEventListener('keydown',this.addHandler  )

}



class Controller {

addHandler(Todo.todos){
 
}
}
